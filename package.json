{
  "name": "argonaut-base-network-pulse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check app.mjs && node --check lib/*.mjs && node --check api/*.mjs && node --check scripts/*.mjs",
    "payanagent:register": "node scripts/register-payanagent.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@x402/core": "2.20.0",
    "@x402/evm": "2.20.0",
    "@x402/express": "2.20.0",
    "@x402/extensions": "2.20.0",
    "express": "5.2.1"
  }
}
